;;; This file was generated by Zebu (Version 3.5.5)

(IN-PACKAGE "ZEBU")
(REQUIRE "zebu-package")
(USE-PACKAGE "ZEBU")

(DEFUN MORE-RHS72 (DUMMY RHS1 MORE-RHS)
  (DECLARE (IGNORE DUMMY))
  (CONS RHS1 MORE-RHS))

(DEFUN CONSTITUENT72 (IDENTIFIER DUMMY STRING)
  (DECLARE (IGNORE DUMMY))
  (MAKE-KLEENE* :-CONSTITUENT IDENTIFIER :-SEPARATOR STRING))

(DEFUN CONSTITUENT73 (IDENTIFIER DUMMY STRING)
  (DECLARE (IGNORE DUMMY))
  (MAKE-KLEENE+ :-CONSTITUENT IDENTIFIER :-SEPARATOR STRING))

(DEFUN RHS173 (CONSTITUENT-LIST)
  (MAKE-PRODUCTION-RHS :-SYNTAX CONSTITUENT-LIST))

(DEFUN RHS174 (CONSTITUENT-LIST DUMMY FEAT-TERM DUMMY1)
  (DECLARE (IGNORE DUMMY1 DUMMY))
  (MAKE-PRODUCTION-RHS
    :-SYNTAX
    CONSTITUENT-LIST
    :-SEMANTICS
    FEAT-TERM))

(DEFUN ZB-RULE74 (NON-TERMINAL DUMMY RHS DUMMY1)
  (DECLARE (IGNORE DUMMY1 DUMMY))
  (MAKE-ZB-RULE :-NAME NON-TERMINAL :-PRODUCTIONS RHS))

(DEFUN LABEL-VALUE-PAIR74 (DUMMY IDENTIFIER FEAT-TERM DUMMY1)
  (DECLARE (IGNORE DUMMY1 DUMMY))
  (MAKE-LABEL-VALUE-PAIR :-LABEL IDENTIFIER :-VALUE FEAT-TERM))

(DEFUN LABEL-VALUE-PAIR75 (DUMMY IDENTIFIER DUMMY1)
  (DECLARE (IGNORE DUMMY1 DUMMY))
  (MAKE-LABEL-VALUE-PAIR :-LABEL IDENTIFIER))

(DEFUN FEAT-TERM75 (IDENTIFIER DUMMY)
  (DECLARE (IGNORE DUMMY))
  (INTERN (CONCATENATE 'STRING (STRING IDENTIFIER) "*")))

(DEFUN FEAT-TERM76 (IDENTIFIER DUMMY)
  (DECLARE (IGNORE DUMMY))
  (INTERN (CONCATENATE 'STRING (STRING IDENTIFIER) "+")))

(DEFUN FEAT-TERM77 (CONJ) (MAKE-FEAT-TERM :-SLOTS CONJ))

(DEFUN CONJ77 (DUMMY LABEL-VALUE-PAIRS DUMMY1)
  (DECLARE (IGNORE DUMMY1 DUMMY))
  LABEL-VALUE-PAIRS)

(DEFUN TYPED-CONJ77 (IDENTIFIER DUMMY CONJ)
  (DECLARE (IGNORE DUMMY))
  (MAKE-FEAT-TERM :-SLOTS CONJ :-TYPE IDENTIFIER))

(DEFUN PRINT-FUNCTION77 (DUMMY DUMMY1 IDENTIFIER DUMMY2)
  (DECLARE (IGNORE DUMMY2 DUMMY1 DUMMY))
  IDENTIFIER)

(DEFUN DEF-TYPE77 (IDENTIFIER DUMMY TYPED-CONJ PRINT-FUNCTION DUMMY1)
  (DECLARE (IGNORE DUMMY1 DUMMY))
  (CONS-DOMAIN-TYPE IDENTIFIER TYPED-CONJ PRINT-FUNCTION))

(DEFUN DEF-TYPE78 (IDENTIFIER DUMMY CONJ PRINT-FUNCTION DUMMY1)
  (DECLARE (IGNORE DUMMY1 DUMMY))
  (CONS-DOMAIN-TYPE IDENTIFIER CONJ PRINT-FUNCTION))

